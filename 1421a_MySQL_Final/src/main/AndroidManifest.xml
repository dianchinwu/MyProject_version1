<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.tcnr08.a0001">

    <uses-permission android:name="tw.tcnr08.m1417.providers.WRITE"></uses-permission>
    <uses-permission android:name="tw.tcnr08.m1417.providers.READ"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <!--NETWORK ip-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <!--WIFI ip-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--不同package哪個資料夾可以共享-->
        <provider
            android:name=".providers.FriendsContentProvider"
            android:authorities="tw.tcnr08.a0001.providers.FriendsContentProvider">
        </provider>

    </application>

</manifest>